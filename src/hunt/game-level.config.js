const GAME_FPS = 30;

const GAME_DIRECTION = {
    LTR: 'left to right',
    RTL: 'right to left'
};

const GAME_LEVEL = [

    // Start, pixels par frame
    [ 2, 1, new GameProfile() ],
    [ 8, 1, new GameProfile() ]
];